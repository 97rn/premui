local Library = {}
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local TextService = game:GetService("TextService")
local Players = game:GetService("Players")

local function GetContainer()
    local success, result = pcall(function() return gethui() end)
    if success and result then return result end
    return game:GetService("CoreGui"):FindFirstChild("RobloxGui") or Players.LocalPlayer:WaitForChild("PlayerGui")
end

local function Tween(instance, properties, duration)
    local info = TweenInfo.new(duration or 0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
    local t = TweenService:Create(instance, info, properties)
    t:Play()
    return t
end

-- Notification System (Advanced)
function Library:Notify(config)
    local Title = config.Title or "Notification"
    local Content = config.Content or ""
    local Duration = config.Time or 5
    
    local container = GetContainer()
    local holder = container:FindFirstChild("NotifyHolder")
    if not holder then
        holder = Instance.new("Frame")
        holder.Name = "NotifyHolder"
        holder.Size = UDim2.new(0, 300, 1, -20)
        holder.Position = UDim2.new(1, -310, 0, 10)
        holder.BackgroundTransparency = 1
        holder.Parent = container
        
        local layout = Instance.new("UIListLayout")
        layout.VerticalAlignment = Enum.VerticalAlignment.Bottom
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 10)
        layout.Parent = holder
    end

    local Notif = Instance.new("Frame")
    Notif.Size = UDim2.new(1, 0, 0, 0)
    Notif.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Notif.ClipsDescendants = true
    Notif.Parent = holder
    
    Instance.new("UICorner", Notif).CornerRadius = UDim.new(0, 8)
    local stroke = Instance.new("UIStroke", Notif)
    stroke.Color = Color3.fromRGB(40, 40, 40)
    
    local tl = Instance.new("TextLabel", Notif)
    tl.Text = Title:upper()
    tl.Font = Enum.Font.GothamBold
    tl.TextSize = 13
    tl.TextColor3 = Color3.fromRGB(255, 255, 255)
    tl.Position = UDim2.fromOffset(10, 8)
    tl.BackgroundTransparency = 1
    
    local cl = Instance.new("TextLabel", Notif)
    cl.Text = Content
    cl.Font = Enum.Font.Gotham
    cl.TextSize = 12
    cl.TextColor3 = Color3.fromRGB(180, 180, 180)
    cl.Position = UDim2.fromOffset(10, 26)
    cl.Size = UDim2.new(1, -20, 0, 0)
    cl.TextWrapped = true
    cl.BackgroundTransparency = 1
    cl.TextXAlignment = Enum.TextXAlignment.Left

    local ts = TextService:GetTextSize(Content, 12, Enum.Font.Gotham, Vector2.new(280, 1000))
    local targetH = ts.Y + 45
    
    Tween(Notif, {Size = UDim2.new(1, 0, 0, targetH)}, 0.4)
    
    task.delay(Duration, function()
        Tween(Notif, {Size = UDim2.new(1, 0, 0, 0), BackgroundTransparency = 1}, 0.4).Completed:Connect(function()
            Notif:Destroy()
        end)
    end)
end

function Library:Create(config)
    local Window = { Tabs = {} }
    
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "AeroUI_" .. math.random(100, 999)
    ScreenGui.Parent = GetContainer()

    -- Reopen Button (Left Side)
    local ReopenBtn = Instance.new("TextButton")
    ReopenBtn.Size = UDim2.new(0, 120, 0, 35)
    ReopenBtn.Position = UDim2.new(0, -130, 0.5, -17)
    ReopenBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    ReopenBtn.Text = "REOPEN UI"
    ReopenBtn.Font = Enum.Font.GothamBold
    ReopenBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    ReopenBtn.Visible = false
    ReopenBtn.Parent = ScreenGui
    Instance.new("UICorner", ReopenBtn)
    Instance.new("UIStroke", ReopenBtn).Color = Color3.fromRGB(40, 40, 40)

    local Main = Instance.new("Frame")
    Main.Size = UDim2.new(0, 580, 0, 400)
    Main.Position = UDim2.new(0.5, -290, 0.5, -200)
    Main.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
    Main.ClipsDescendants = true
    Main.Parent = ScreenGui
    Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)
    
    -- Functionality
    ReopenBtn.MouseButton1Click:Connect(function()
        ReopenBtn.Visible = false
        Main.Visible = true
        Tween(Main, {Size = UDim2.new(0, 580, 0, 400)}, 0.4)
    end)

    -- Control Buttons
    local Close = Instance.new("TextButton", Main)
    Close.Text = "×"
    Close.Size = UDim2.new(0, 30, 0, 30)
    Close.Position = UDim2.new(1, -35, 0, 5)
    Close.BackgroundTransparency = 1
    Close.TextColor3 = Color3.fromRGB(200, 50, 50)
    Close.TextSize = 20
    Close.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

    local Min = Instance.new("TextButton", Main)
    Min.Text = "−"
    Min.Size = UDim2.new(0, 30, 0, 30)
    Min.Position = UDim2.new(1, -65, 0, 5)
    Min.BackgroundTransparency = 1
    Min.TextColor3 = Color3.fromRGB(200, 200, 200)
    Min.TextSize = 20
    Min.MouseButton1Click:Connect(function()
        Tween(Main, {Size = UDim2.new(0, 0, 0, 0)}, 0.4).Completed:Wait()
        Main.Visible = false
        ReopenBtn.Visible = true
        Tween(ReopenBtn, {Position = UDim2.new(0, 10, 0.5, -17)}, 0.4)
    end)

    local Sidebar = Instance.new("Frame", Main)
    Sidebar.Size = UDim2.new(0, 160, 1, -40)
    Sidebar.Position = UDim2.new(0, 10, 0, 35)
    Sidebar.BackgroundTransparency = 1
    
    local TabList = Instance.new("UIListLayout", Sidebar)
    TabList.Padding = UDim.new(0, 5)

    local PageContainer = Instance.new("Frame", Main)
    PageContainer.Size = UDim2.new(1, -190, 1, -50)
    PageContainer.Position = UDim2.new(0, 180, 0, 40)
    PageContainer.BackgroundTransparency = 1

    function Window:Tab(name, icon)
        local Tab = { Elements = {} }
        
        local TabBtn = Instance.new("TextButton", Sidebar)
        TabBtn.Size = UDim2.new(1, 0, 0, 35)
        TabBtn.BackgroundTransparency = 1
        TabBtn.Text = ""
        
        local TabIcon = Instance.new("ImageLabel", TabBtn)
        TabIcon.Size = UDim2.new(0, 18, 0, 18)
        TabIcon.Position = UDim2.new(0, 10, 0.5, -9)
        TabIcon.Image = icon or ""
        TabIcon.BackgroundTransparency = 1
        TabIcon.ImageColor3 = Color3.fromRGB(150, 150, 150)
        
        local TabLabel = Instance.new("TextLabel", TabBtn)
        TabLabel.Text = name
        TabLabel.Size = UDim2.new(1, -40, 1, 0)
        TabLabel.Position = UDim2.new(0, 35, 0, 0)
        TabLabel.BackgroundTransparency = 1
        TabLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
        TabLabel.Font = Enum.Font.GothamMedium
        TabLabel.TextXAlignment = Enum.TextXAlignment.Left

        local Page = Instance.new("ScrollingFrame", PageContainer)
        Page.Size = UDim2.new(1, 0, 1, 0)
        Page.BackgroundTransparency = 1
        Page.Visible = false
        Page.ScrollBarThickness = 2
        Instance.new("UIListLayout", Page).Padding = UDim.new(0, 8)

        TabBtn.MouseButton1Click:Connect(function()
            for _, t in pairs(Sidebar:GetChildren()) do
                if t:IsA("TextButton") then
                    Tween(t.TextLabel, {TextColor3 = Color3.fromRGB(150, 150, 150)})
                    Tween(t.ImageLabel, {ImageColor3 = Color3.fromRGB(150, 150, 150)})
                end
            end
            for _, p in pairs(PageContainer:GetChildren()) do p.Visible = false end
            Page.Visible = true
            Tween(TabLabel, {TextColor3 = Color3.fromRGB(255, 255, 255)})
            Tween(TabIcon, {ImageColor3 = Color3.fromRGB(255, 255, 255)})
        end)

        function Tab:Slider(name, min, max, def, rounding, callback)
            local SFrame = Instance.new("Frame", Page)
            SFrame.Size = UDim2.new(1, -5, 0, 50)
            SFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
            Instance.new("UICorner", SFrame)
            
            local Label = Instance.new("TextLabel", SFrame)
            Label.Text = name
            Label.Position = UDim2.fromOffset(10, 5)
            Label.TextColor3 = Color3.fromRGB(255, 255, 255)
            Label.BackgroundTransparency = 1
            
            local ValLabel = Instance.new("TextLabel", SFrame)
            ValLabel.Text = tostring(def)
            ValLabel.Position = UDim2.new(1, -50, 0, 5)
            ValLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
            ValLabel.BackgroundTransparency = 1

            local Bar = Instance.new("Frame", SFrame)
            Bar.Size = UDim2.new(1, -20, 0, 5)
            Bar.Position = UDim2.new(0, 10, 1, -15)
            Bar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            
            local Fill = Instance.new("Frame", Bar)
            Fill.Size = UDim2.fromScale((def-min)/(max-min), 1)
            Fill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)

            local function Update(input)
                local pos = math.clamp((input.Position.X - Bar.AbsolutePosition.X) / Bar.AbsoluteSize.X, 0, 1)
                local val = min + (max - min) * pos
                if rounding then val = math.floor(val / rounding + 0.5) * rounding end
                ValLabel.Text = tostring(val)
                Fill.Size = UDim2.fromScale(pos, 1)
                callback(val)
            end

            Bar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    local move; move = UserInputService.InputChanged:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                            Update(input)
                        end
                    end)
                    UserInputService.InputEnded:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                            move:Disconnect()
                        end
                    end)
                    Update(input)
                end
            end)
        end

        function Tab:Keybind(name, def, callback)
            local current = def
            local KFrame = Instance.new("Frame", Page)
            KFrame.Size = UDim2.new(1, -5, 0, 40)
            KFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
            Instance.new("UICorner", KFrame)

            local Label = Instance.new("TextLabel", KFrame)
            Label.Text = name
            Label.Position = UDim2.fromOffset(10, 0)
            Label.Size = UDim2.new(1, 0, 1, 0)
            Label.TextColor3 = Color3.fromRGB(255, 255, 255)
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.BackgroundTransparency = 1

            local BindBtn = Instance.new("TextButton", KFrame)
            BindBtn.Size = UDim2.new(0, 80, 0, 25)
            BindBtn.Position = UDim2.new(1, -90, 0.5, -12)
            BindBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
            BindBtn.Text = current.Name
            BindBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            Instance.new("UICorner", BindBtn)

            BindBtn.MouseButton1Click:Connect(function()
                BindBtn.Text = "..."
                local connection; connection = UserInputService.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.Keyboard then
                        current = input.KeyCode
                        BindBtn.Text = current.Name
                        connection:Disconnect()
                    end
                end)
            end)

            UserInputService.InputBegan:Connect(function(input, gpe)
                if not gpe and input.KeyCode == current then callback() end
            end)
        end

        function Tab:Paragraph(config)
            local PFrame = Instance.new("Frame", Page)
            PFrame.Size = UDim2.new(1, -5, 0, 60)
            PFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
            Instance.new("UICorner", PFrame)

            local title = Instance.new("TextLabel", PFrame)
            title.Text = (config.Title or "Info"):upper()
            title.Font = Enum.Font.GothamBold
            title.TextColor3 = Color3.fromRGB(255, 255, 255)
            title.Position = UDim2.fromOffset(10, 5)
            title.BackgroundTransparency = 1

            local desc = Instance.new("TextLabel", PFrame)
            desc.Text = config.Content or ""
            desc.Size = UDim2.new(1, -20, 0, 0)
            desc.Position = UDim2.fromOffset(10, 25)
            desc.TextColor3 = Color3.fromRGB(160, 160, 160)
            desc.TextWrapped = true
            desc.TextXAlignment = Enum.TextXAlignment.Left
            desc.BackgroundTransparency = 1
            
            local ts = TextService:GetTextSize(desc.Text, 12, Enum.Font.Gotham, Vector2.new(350, 1000))
            PFrame.Size = UDim2.new(1, -5, 0, ts.Y + 40)
        end

        -- ... Add existing Button/Toggle code here ...

        return Tab
    end

    function Window:SettingManager()
        local Tab = self:Tab("Settings", "rbxassetid://6031280882")
        Tab:Paragraph({Title = "UI Management", Content = "Control the visibility and lifecycle of the interface."})
        -- Add Unload Logic
        return Tab
    end

    return Window
end

return Library
